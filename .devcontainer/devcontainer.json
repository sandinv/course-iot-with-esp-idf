{
    "$schema": "https://raw.githubusercontent.com/devcontainers/spec/main/schemas/devContainer.schema.json",
    "name": "ESP-IDF Development Environment",
    "build": {
        "dockerfile": "Dockerfile",
        "context": "..",
        "args": {
            "IDF_VERSION": "5.4.1",
            "IDF_TARGETS": "esp32",
            "INSTALL_MOSQUITTO": "false"
        }
    },
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-vscode.cpptools",
                "ms-vscode.cmake-tools",
                "ms-vscode.hexeditor"
            ]
        }
    },
    "workspaceMount": "source=${localWorkspaceFolder},target=/workspace/project,type=bind,consistency=cached",
    "workspaceFolder": "/workspace/esp-idf.code-workspace",
    "runArgs": [
        "--network=bridge"
    ],
    "remoteUser": "root"
}
